x: int = bar.foo()
x: int = bar.foo(1)
x: int = bar.foo(1, -4)
x: int = bar.foo(test())
x: int = bar.foo(bar.egg())

cls Test
    new Test()
        m_x = 10
    end

    fun hiThere(x: int) -> int
        ret (x * 2)
    end
end

lol: Test = new Test()
